package {{package_name}}

import android.app.Application
import dagger.hilt.android.HiltAndroidApp

/**
 * Application class for {{app_name}}.
 * Initializes Hilt for dependency injection.
 */
@HiltAndroidApp
class {{app_name}}Application : Application() {
    
    override fun onCreate() {
        super.onCreate()
        
        // Initialize any application-wide components here
        // Examples:
        // - Analytics
        // - Crash reporting
        // - Logging
        // - Theme/Dark mode preferences
    }
}